# M5Stack CoreS3-Lite Setup Guide

Полное руководство по прошивке M5Stack CoreS3-Lite и настройке CuteAssistantGPT.

---

## 📋 Содержание

1. [Требования](#требования)
2. [Установка PlatformIO](#установка-platformio)
3. [Получение OpenAI API ключа](#получение-openai-api-ключа)
4. [Настройка WiFi и API ключей](#настройка-wifi-и-api-ключей)
5. [Прошивка устройства](#прошивка-устройства)
6. [Первый запуск](#первый-запуск)
7. [Устранение проблем](#устранение-проблем)

---

## 1. Требования

### Оборудование
- ✅ **M5Stack CoreS3-Lite** (ESP32-S3, 320x240 IPS дисплей)
- ✅ **USB-C кабель** (для подключения к компьютеру)
- ✅ **microSD карта** (для хранения WiFi credentials и API ключей)
- ⚠️ **Датчики** (опционально):
  - BME688 Environmental Sensor
  - ADXL345 Accelerometer
  - RFID 2 UNIT Reader
  - Hall Effect Sensor

### Программное обеспечение
- ✅ **Visual Studio Code** (рекомендуется)
- ✅ **PlatformIO IDE** (расширение для VS Code)
- ✅ **Git** (для клонирования репозитория)
- ✅ **Python 3.7+** (для PlatformIO)

### Аккаунты
- ✅ **OpenAI Account** с API доступом
- ✅ Платёжный метод для OpenAI API (требуется пополнение баланса)

---

## 2. Установка PlatformIO

### Шаг 1: Установка Visual Studio Code

1. Скачайте VS Code: https://code.visualstudio.com/
2. Установите для вашей ОС (Windows/macOS/Linux)

### Шаг 2: Установка расширения PlatformIO

1. Откройте VS Code
2. Нажмите `Ctrl+Shift+X` (или `Cmd+Shift+X` на macOS)
3. Найдите "PlatformIO IDE"
4. Нажмите **Install**
5. Перезапустите VS Code

### Шаг 3: Проверка установки

1. В VS Code должна появиться иконка "домик" на левой панели (PlatformIO)
2. Нажмите на неё → выберите **PIO Home** → **Open**
3. Вы должны увидеть стартовую страницу PlatformIO

---

## 3. Получение OpenAI API ключа

### Шаг 1: Создание аккаунта OpenAI

1. Перейдите на https://platform.openai.com/signup
2. Зарегистрируйтесь через email или Google/Microsoft аккаунт
3. Подтвердите email адрес

### Шаг 2: Пополнение баланса

⚠️ **Важно**: Новые аккаунты больше не имеют бесплатных кредитов.

1. Откройте https://platform.openai.com/settings/organization/billing/overview
2. Нажмите **Add payment method**
3. Добавьте банковскую карту
4. Пополните баланс минимум на **$5** (рекомендуется $10-20)

**Примерная стоимость**:
- GPT-4o-audio-preview: ~$0.03-0.05 за запрос (зависит от длины аудио)
- 100 запросов ≈ $3-5

### Шаг 3: Создание API ключа

1. Перейдите в https://platform.openai.com/api-keys
2. Нажмите **+ Create new secret key**
3. Введите имя ключа (например, "CuteAssistantGPT_CoreS3")
4. Нажмите **Create secret key**
5. **⚠️ СКОПИРУЙТЕ КЛЮЧ СРАЗУ!** Он больше не будет показан

Формат ключа: `sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`

### Шаг 4: Сохранение ключа

Сохраните ключ в надёжном месте:
- Менеджер паролей (1Password, Bitwarden)
- Зашифрованный файл
- **НЕ ПУБЛИКУЙТЕ** в GitHub или публичных местах!

---

## 4. Настройка WiFi и API ключей

### Шаг 1: Подготовка microSD карты

1. **Форматируйте** microSD карту в **FAT32**
2. Вставьте карту в картридер компьютера

### Шаг 2: Создание файла wifi.txt

Создайте файл `wifi.txt` в корне SD карты:

```txt
# WiFi Configuration for CuteAssistantGPT
# Format: NAME, SSID, PASSWORD

NAME=Home WiFi
SSID=MyHomeNetwork
PASSWORD=MySecurePassword123
---

# You can add up to 3 networks
# NAME=Work WiFi
# SSID=OfficeNetwork
# PASSWORD=WorkPassword456
# ---

# NAME=Mobile Hotspot
# SSID=iPhone_Hotspot
# PASSWORD=HotspotPass789
# END
```

**Правила**:
- Одна сеть = блок из 3 строк (NAME, SSID, PASSWORD)
- Разделитель между сетями: `---` или `END`
- Комментарии начинаются с `#` или `//`
- Максимум 3 сети
- Устройство попробует подключиться к первой доступной

**Пример для русских SSID** (с UTF-8):
```txt
NAME=Дом
SSID=Мой_WiFi_2.4GHz
PASSWORD=СуперПароль2024
---
```

### Шаг 3: Создание файла apis.txt

Создайте файл `apis.txt` в корне SD карты:

```txt
# OpenAI API Key
OPENAI=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

**⚠️ Замените** `sk-proj-XXX...` на ваш реальный API ключ из шага 3!

### Шаг 4: Безопасное извлечение SD карты

1. **Windows**: "Безопасное извлечение устройства"
2. **macOS**: Eject в Finder
3. **Linux**: `umount /dev/sdX`

### Шаг 5: Вставка SD карты в CoreS3-Lite

1. Найдите слот microSD на **задней** стороне CoreS3-Lite
2. Вставьте карту **контактами вниз**
3. Протолкните до щелчка

---

## 5. Прошивка устройства

### Шаг 1: Клонирование репозитория

Откройте терминал:

```bash
git clone https://github.com/Romati21/kodedot_SharedExamples.git
cd kodedot_SharedExamples/CuteAssistantGPT
```

### Шаг 2: Открытие проекта в PlatformIO

1. Откройте VS Code
2. **File** → **Open Folder...**
3. Выберите папку `CuteAssistantGPT`
4. PlatformIO автоматически обнаружит `platformio.ini`

### Шаг 3: Установка зависимостей

PlatformIO автоматически скачает все библиотеки при первой компиляции:
- M5Unified 0.1.16
- LVGL 9.0.0
- Adafruit BME680
- Adafruit ADXL345
- ArduinoJson 7.0
- И другие...

**Подождите ~5-10 минут** при первой компиляции.

### Шаг 4: Подключение CoreS3-Lite

1. Подключите CoreS3-Lite к компьютеру через **USB-C**
2. Включите устройство (кнопка питания сбоку)
3. Устройство должно определиться как **USB Serial Device**

**Проверка порта** (терминал):
- **Windows**: Диспетчер устройств → Порты (COM и LPT) → `COM3`, `COM4`, и т.д.
- **macOS**: `ls /dev/cu.usbserial-*` или `/dev/cu.usbmodem*`
- **Linux**: `ls /dev/ttyUSB*` или `/dev/ttyACM*`

### Шаг 5: Компиляция и прошивка

#### Вариант A: Через GUI (рекомендуется)

1. Внизу VS Code найдите синюю панель PlatformIO
2. Нажмите **иконку "стрелка вправо"** (Upload)
3. В появившемся меню выберите **env:cores3_lite**
4. Дождитесь компиляции и прошивки

#### Вариант B: Через терминал

```bash
# Компиляция для CoreS3-Lite
pio run -e cores3_lite

# Прошивка
pio run -e cores3_lite --target upload

# Или всё сразу
pio run -e cores3_lite -t upload
```

### Шаг 6: Ожидание прошивки

Вы увидите вывод:
```
Connecting........_____.
Chip is ESP32-S3 (revision v0.1)
Features: WiFi, BLE
Crystal is 40MHz
...
Writing at 0x00410000... (100%)
Wrote 1638400 bytes (1035291 compressed) at 0x00400000 in 15.2 seconds
Hash of data verified.

Leaving...
Hard resetting via RTS pin...
```

✅ **Прошивка завершена!**

---

## 6. Первый запуск

### Шаг 1: Открытие Serial Monitor

**Вариант A: PlatformIO Serial Monitor**
1. Нажмите иконку **"штепсель"** на синей панели внизу
2. Или: `pio device monitor -e cores3_lite`

**Вариант B: Встроенный VS Code терминал**
```bash
pio device monitor -b 115200
```

### Шаг 2: Наблюдение загрузки

Вы увидите последовательность:

```
CuteAssistant starting...
[Display] Initializing CoreS3 display (320x240)...
[UIManager] Initialized successfully
[Setup] Initializing sensors...
[SensorManager] Initializing I2C bus...
[SensorManager] Scanning for sensors...
[SensorManager] BME688 found at 0x77
[SensorManager] ADXL345 found at 0x53
[SensorManager] Detected sensors: BME688, ADXL345
[WiFiManager] Loading WiFi credentials from SD...
[WiFiManager] Loaded WiFi #1: Home WiFi (MyHomeNetwork)
[WiFiManager] Attempting WiFi connection...
[WiFiManager] Connecting to: Home WiFi (MyHomeNetwork) - Attempt 1/2
........
[WiFiManager] Connected to Home WiFi
[WiFiManager] IP: 192.168.1.100
[WiFiManager] RSSI: -45 dBm
[WiFiManager] Loading API keys from SD (/apis.txt)...
[WiFiManager] OPENAI key loaded (56 chars)
[AudioManager] I2S initialized on port 1
[Setup] System ready!
```

### Шаг 3: Проверка экрана

На дисплее CoreS3-Lite должны появиться:
- 👀 **Два анимированных глаза** (белые кружки)
- 🔵 **GPT логотип** в правом верхнем углу
- 🔋 **Процент батареи** (если на батарее)

Глаза должны **моргать** и **двигаться**.

### Шаг 4: Первый голосовой запрос

1. **Нажмите и УДЕРЖИВАЙТЕ** кнопку **G** (центральная кнопка под дисплеем)
2. Глаза станут **красными** (режим записи)
3. **Говорите**: "Hello! What is your name?"
4. **Отпустите** кнопку G
5. Глаза станут **фиолетовыми** (обработка)
6. Дождитесь ответа на экране

**Ожидаемый результат**:
```
Response: "Hi there! I'm CuteAssistant, your friendly AI companion!"
```

### Шаг 5: Тест датчиков (если подключены)

**Температура**:
```
Вы: "What's the temperature?"
AI: "The current temperature is 23.5 degrees Celsius."
```

**Наклон**:
```
Вы: "Am I tilted?"
AI: "You're tilted at 0.82 g on the X axis."
```

**Магнит** (поднесите магнит к Hall сенсору):
```
Вы: "Is there a magnet nearby?"
AI: "Yes! Magnetic field is detected."
```

### Шаг 6: Тест GPIO команд

```
Вы: "Turn on pin 5"
AI: "Sure! I've activated pin 5 for you."
Actions: BEGIN;GPIO(5,ON);END
```

GPIO 5 должен выдать **3.3V** (проверьте мультиметром).

---

## 7. Устранение проблем

### Проблема: Устройство не подключается к WiFi

**Симптомы**:
- Глаза остаются синими (Connecting)
- Serial log: `[WiFiManager] Connection failed`

**Решения**:

1. **Проверьте wifi.txt**:
   ```bash
   # Убедитесь что файл в корне SD:
   /wifi.txt  (не /config/wifi.txt)

   # Проверьте формат:
   NAME=Home WiFi    # Без пробелов вокруг =
   SSID=MyNetwork    # Точное имя сети
   PASSWORD=Pass123  # Без пробелов в начале/конце
   ---               # Разделитель
   ```

2. **Проверьте SSID сети**:
   - Используйте **2.4GHz WiFi** (CoreS3 не поддерживает 5GHz!)
   - Проверьте регистр букв (WiFi_5G ≠ wifi_5g)
   - Избегайте спецсимволов в SSID

3. **Проверьте SD карту**:
   ```
   [WiFiManager] ERROR: /wifi.txt not found on SD
   ```
   - Переформатируйте SD в FAT32
   - Проверьте что карта вставлена до щелчка
   - Попробуйте другую SD карту

4. **Слабый сигнал WiFi**:
   ```
   [WiFiManager] RSSI: -75 dBm  # Слабый сигнал
   ```
   - Поднесите устройство ближе к роутеру
   - RSSI > -70 dBm — хорошо
   - RSSI < -80 dBm — плохо

### Проблема: OpenAI API ошибки

**401 Unauthorized**:
```
[BasicGPTClient] HTTP error 401: Unauthorized
Response: Check API key
```
**Решение**: API ключ неверный или истёк
- Проверьте apis.txt (нет лишних пробелов)
- Создайте новый ключ на platform.openai.com/api-keys
- Убедитесь что скопировали полный ключ (начинается с `sk-proj-`)

**429 Too Many Requests**:
```
[BasicGPTClient] HTTP error 429
Response: Too many requests
```
**Решение**: Превышен rate limit
- Подождите 1 минуту
- Проверьте usage: https://platform.openai.com/usage
- Повысьте tier на https://platform.openai.com/settings/organization/limits

**Insufficient Quota**:
```
Response: OpenAI unavailable
```
**Решение**: Баланс исчерпан
- Проверьте баланс: https://platform.openai.com/settings/organization/billing/overview
- Пополните минимум на $5

### Проблема: Микрофон не записывает

**Симптомы**:
- Нажимаете G, но глаза не краснеют
- Serial log: `[AudioManager] ERROR: I2S failed`

**Решения**:

1. **Проверьте M5.begin()**:
   - M5Unified должен инициализировать микрофон автоматически
   - В main.cpp должен быть вызов M5.begin()

2. **Проверьте GPIO 14**:
   - GPIO 14 зарезервирован для микрофона (I2S DIN)
   - НЕ подключайте ничего к GPIO 14!

3. **Проверьте настройки I2S**:
   ```cpp
   // В main.cpp должно быть:
   audioConfig.sckPin = MIC_I2S_SCK;   // GPIO 34
   audioConfig.wsPin = MIC_I2S_WS;     // GPIO 33
   audioConfig.dinPin = MIC_I2S_DIN;   // GPIO 14
   ```

### Проблема: Датчики не обнаруживаются

**Симптомы**:
```
[SensorManager] WARNING: No sensors detected
```

**Решения**:

1. **Проверьте I2C подключение**:
   - SDA = GPIO 11 (жёлтый провод Grove)
   - SCL = GPIO 12 (белый провод Grove)
   - VCC = 3.3V (красный)
   - GND = GND (чёрный)

2. **I2C сканирование**:
   Добавьте в setup():
   ```cpp
   Wire.begin(11, 12);
   for (uint8_t addr = 1; addr < 127; addr++) {
       Wire.beginTransmission(addr);
       if (Wire.endTransmission() == 0) {
           Serial.printf("I2C device found at 0x%02X\n", addr);
       }
   }
   ```

3. **Проверьте адреса**:
   - BME688: 0x76 или 0x77 (переключается перемычкой SDO)
   - ADXL345: 0x53 (ALT ADDRESS = LOW)
   - RFID: 0x28 (фиксированный)

4. **Попробуйте отдельно**:
   - Подключите только один датчик
   - Исключите конфликты адресов

### Проблема: Экран пустой/белый

**Решения**:

1. **Перезагрузите устройство**:
   - Длительное нажатие кнопки питания (10 сек)
   - Отключите и подключите USB

2. **Проверьте M5Unified**:
   ```cpp
   // В display_manager.cpp должно быть:
   M5.begin();
   M5.Display.setRotation(1);  // Landscape
   ```

3. **Проверьте LVGL**:
   ```
   [Display] LVGL buffer size: 76800 bytes
   [UIManager] Initialized successfully
   ```
   Если ошибка — недостаточно PSRAM.

### Проблема: Сборка не удаётся (compilation errors)

**Ошибка: M5Unified version**:
```
#error "M5Unified version too old. Minimum required: 0.1.16"
```
**Решение**: Обновите M5Unified:
```bash
pio pkg update
# Или вручную в platformio.ini:
m5stack/M5Unified @ ^0.1.16
```

**Ошибка: Missing library**:
```
fatal error: Adafruit_BME680.h: No such file or directory
```
**Решение**: Установите зависимости:
```bash
pio pkg install
```

**Ошибка: Out of memory**:
```
region `iram0_0_seg' overflowed
```
**Решение**: Недостаточно flash памяти
- Проверьте partitions.csv (должно быть 16MB)
- Уменьшите LVGL buffer size в display_manager.cpp

---

## 📚 Дополнительные ресурсы

### Документация
- **M5Stack CoreS3 Docs**: https://docs.m5stack.com/en/core/CoreS3
- **OpenAI API Docs**: https://platform.openai.com/docs/api-reference
- **PlatformIO Docs**: https://docs.platformio.org/

### Полезные команды PlatformIO

```bash
# Компиляция без прошивки
pio run -e cores3_lite

# Прошивка
pio run -e cores3_lite -t upload

# Serial Monitor
pio device monitor -b 115200

# Очистка сборки
pio run -t clean

# Обновление библиотек
pio pkg update

# Показать размер прошивки
pio run -e cores3_lite -t size
```

### Поддержка

- **Issues**: https://github.com/Romati21/kodedot_SharedExamples/issues
- **Discussions**: https://github.com/Romati21/kodedot_SharedExamples/discussions

---

## ✅ Чек-лист успешной настройки

- [ ] PlatformIO установлен в VS Code
- [ ] Репозиторий клонирован
- [ ] OpenAI API ключ получен и баланс пополнен
- [ ] wifi.txt создан на SD карте с правильным SSID/паролем
- [ ] apis.txt создан с OpenAI ключом
- [ ] SD карта вставлена в CoreS3-Lite
- [ ] Прошивка успешно загружена
- [ ] Устройство подключилось к WiFi (IP адрес в Serial)
- [ ] Глаза появились на экране и анимируются
- [ ] Первый голосовой запрос работает
- [ ] Датчики обнаружены (если подключены)

**Поздравляем! CuteAssistantGPT готов к использованию! 🎉**
